/**
 * ローカル確認用の設定例です。
 * 1. このファイルをコピーして config.js を作成
 * 2. YOUR_GAS_WEB_APP_URL_HERE を実際のWebアプリURLに差し替え
 *
 * ※ config.js は .gitignore され、GitHubには上がりません。
 * ※ 本番（GitHub Pages）では GitHub Secrets + Actions が config.js を生成します。
 */
window.NSTYLE_CONFIG = {
  gasEndpoint: 'YOUR_GAS_WEB_APP_URL_HERE'
};
